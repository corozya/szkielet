# Copy this file to `.env.memory` if you want to override the default path.
# Keep the resulting `.env.memory` untracked.

# Default: ./.memory/memory.jsonl
MEMORY_FILE_PATH=/absolute/or/relative/path/to/memory.jsonl
