# memory-mcp — instalacja

**Typ:** MCP | **Opis:** Trwała pamięć dla agentów AI między sesjami

**Pliki:**
- `scripts/run-memory-mcp.sh`

**MCP entry:**
```json
{ "command": "bash", "args": ["scripts/run-memory-mcp.sh"], "cwd": "." }
```

**Setup:** brak — zapisuje do `.memory/memory.jsonl`
