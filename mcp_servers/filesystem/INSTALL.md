# filesystem-mcp — instalacja

**Typ:** MCP | **Opis:** Dostęp do plików projektu dla agentów AI

**Pliki:**
- `scripts/run-filesystem-mcp.sh`

**MCP entry:**
```json
{ "command": "bash", "args": ["scripts/run-filesystem-mcp.sh"], "cwd": "." }
```

**Setup:** brak
