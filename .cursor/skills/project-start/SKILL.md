---
name: project-start
description: One command onboarding: attach repos into apps/, ensure apps/ gitignored, then ALWAYS run npm run init-kb.
---

# project-start

Run `npm run start-project`.

It will:
- attach repos under `apps/`
- ensure `apps/` is gitignored in the skeleton
- run `npm run init-kb` (always)

